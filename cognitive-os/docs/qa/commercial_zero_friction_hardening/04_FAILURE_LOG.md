# 04 Failure Log

| ID | Severidad | Estado | Evidencia | Reproduccion | Causa raiz | Fix | Validacion |
|---|---|---|---|---|---|---|---|
| FAIL-001 | P1 | corregido | `scripts/full-qa.sh` toleraba `alembic check` fallido como `WARN` | Leer script pre-fix | Alembic tratado como opcional aun con DB configurada | Alembic hard-fail si hay `.env`, `.env.local` o `DATABASE_URL`; skip solo sin config DB | `bash scripts/full-qa.sh` -> verde |
| FAIL-002 | P2 | corregido | Sidebar pintaba `configured` como `danger` | Playwright `health-verified-vs-configured` contra mock `configured` | Branch visual no contemplaba estado honesto `configured` | `configured` ahora usa `warn`; regression acepta `configured` | `bash scripts/full-e2e.sh` -> 31 passed |
| FAIL-003 | P2 | corregido | `regression-critical.spec.ts` solo aceptaba `ok/degraded` | E2E health dashboard con overall `configured` | Spec obsoleto frente a contrato configured-vs-verified | Aceptar `configured` como estado valido | `bash scripts/full-e2e.sh` -> 31 passed |
| FAIL-004 | P2 | corregido | ErrorBoundary: `(jobs ?? []) is not iterable` | `error-empty-loading-states.spec.ts` con `/jobs` devolviendo `{}` | `NotificationCenter` iteraba payloads no verificados | `asArray` para approvals/jobs/audit | Spec focal y `full-e2e` verdes |
| FAIL-005 | P1 | corregido | `full-qa.sh` fallaba lint-eando `.next-qa` generado | Ejecutar `full-qa` con `.next-qa` preexistente | ESLint no ignoraba dist QA y script no limpiaba al inicio | Ignorar `.next-qa/**` y limpiar antes de lint/build | `bash scripts/full-qa.sh` -> verde |
| FAIL-006 | P1 | corregido | `full-e2e.sh` hacia `npm ci` aunque exigia server vivo; Next dev moria | Correr `full-e2e` contra dev server del mismo arbol | Instalacion de deps durante runtime invalida `node_modules` | `npm ci` ahora opt-in (`COGOS_E2E_NPM_CI=1`) y se verifica server despues | `bash scripts/full-e2e.sh` -> 31 passed |
| FAIL-007 | P2 | corregido | CORS bloqueaba frontend local en `:3101` | `full-e2e` con `COGOS_BASE_URL=http://localhost:3101` | Defaults CORS solo cubrian `3000/3001`; friccion local innecesaria | Defaults agregan `localhost/127.0.0.1:3101`; `full-e2e` hace preflight CORS diagnostico | `test_config.py`, `full-e2e` con API temporal `:8001` |
| FAIL-008 | P2 | corregido | `forms.spec.ts` hardcodeaba API `:8000` y rompia gates con API alternativa | E2E con `COGOS_API_BASE=http://127.0.0.1:8001` | Test no respetaba env del runner | Usa `process.env.COGOS_API_BASE` | `full-e2e` -> 31 passed |
| FAIL-009 | externo | mitigado/ejecutado | TestSprite bootstrap timeout 120s; CLI inicial fallo por falta de `testsprite_frontend_test_plan.json` | `testsprite_bootstrap`, `testsprite_generate_frontend_test_plan`, CLI `generateCodeAndExecute` | MCP/servicio TestSprite dejo bootstrap colgado y requirio PRD/plan manual | Se genero PRD + plan, se ejecuto subconjunto `TC001/TC002/TC005` | TestSprite 3/3 passed como smoke advisory; no sustituye Playwright |
| FAIL-010 | P2 | cerrado | Live-readonly figuraba como no ejecutado en reporte final anterior | `LIVE_TESTS_ENABLED=1 bash scripts/full-qa-live.sh` | Gate opt-in no se habia corrido en la primera pasada | Ejecutado con proveedores reales read-only | 8 passed, 2 warnings MCP deprecations |
