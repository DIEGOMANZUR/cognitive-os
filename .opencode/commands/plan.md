---
description: Create a safe implementation plan before editing
agent: plan
---

Use the `planning-with-files` skill.

Task:
$ARGUMENTS

Create or update:

- `task_plan.md`
- `findings.md`
- `progress.md`

Do not edit implementation files yet.
Identify risks, unknowns, required MCPs, required credentials, and validation
commands. Reference real files with `path:line` when useful.
